# Supabase public browser configuration. Never commit a populated .env file.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public deployment origin used only for canonical URLs and redirect allow-lists.
VITE_APP_ORIGIN=
