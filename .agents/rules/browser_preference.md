# Browser Preference Rule

- When opening a browser for local preview, web app URLs, or documentation, always launch **Google Chrome** by default using:
  `open -a "/Applications/Google Chrome 2.app" <URL>` (or `open -b com.google.Chrome <URL>`).
- Use Safari only when the user explicitly requests it.
